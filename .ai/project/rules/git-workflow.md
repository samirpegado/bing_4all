# Git Workflow Rules

- Use small, reviewable commits.
- Prefer descriptive branch and PR names.
- Update docs when architecture, stack, or requirements change.
- Treat `AGENTS.md`, `.cursor/rules`, `.github/*`, and `.kiro/*` as code-reviewed assets.
