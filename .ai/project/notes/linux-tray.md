# Linux tray (pendente)

A bandeja com `tray_manager` exige no host:

```bash
sudo apt install libayatana-appindicator3-dev
```

Enquanto isso, o app usa `window_manager`: fechar a janela apenas oculta o processo (útil para atualização em segundo plano).
