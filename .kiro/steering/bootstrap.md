---
inclusion: auto
name: project-bootstrap
description: Bootstrap or refresh the repository AI context by reading AGENTS.md, scanning the project, generating inventory and stack summaries, and asking only missing business questions.
---

# Bootstrap Steering

Follow the bootstrap workflow from `.ai/manifest.yaml`.
Prefer running `python scripts/bootstrap_ai_context.py --write` when possible.
